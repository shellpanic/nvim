-- local highlight = {
--    "CursorColumn",
--    "Whitespace",
-- }

require("ibl").setup({
   -- enabled = true,
   -- indent = { highlight = highlight, char = "", tab_char = "➡" },
   -- whitespace = {
   -- enabled = true,
   --    highlight = highlight,
   --    remove_blankline_trail = false,
   -- },
   -- scope = { enabled = false }, -- alternating higlighting the scopes
})
